// Matches the Paper "Main Canvas" artboard (4794x2961) exactly, plus room
// below it for the closing chapter (which isn't part of that artboard).
export const WORLD_WIDTH = 4794;
export const WORLD_HEIGHT = 3900;

export const ORIGIN = { x: WORLD_WIDTH / 2, y: 1560 };

export const MIN_SCALE = 0.35;
export const MAX_SCALE = 1.6;
export const DEFAULT_SCALE = 0.62;
